fallback: true
intents:
    fallbackIntent:
        fallback: true
states:
    init:
        initial: true
        transitions:
            init:
                fallback: true
        action:
            name: fallbackAction
        end: true

actions:
    fallbackAction:
        type: text
        options:
            text: "Maaf, kami tidak paham maksud Anda"